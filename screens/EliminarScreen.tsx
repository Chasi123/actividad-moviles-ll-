import { StyleSheet, Text, View } from 'react-native'
import React, { useState } from 'react'

export default function EliminarScreen() {
    const [cedula, setcedula] = useState("second")
  return (
    <View>
      <Text>Eliminar</Text>
    </View>
  )
}

const styles = StyleSheet.create({})